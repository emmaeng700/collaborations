import cv2
import numpy as np

# TODO (Oppong): Load trained model from model/ directory
# Run OpenCV preprocessing + inference
# Return { disease_label, confidence }

def classify(image_bytes: bytes) -> dict:
    # placeholder — replace with actual model inference
    return {"disease": "", "confidence": 0.0}
